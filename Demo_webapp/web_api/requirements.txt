# Deps for app.py (the "api" service). Kept separate from
# requirements.txt (the frontend/Streamlit deps) so each container only
# installs what it actually runs -- the Streamlit image never needs fastapi,
# and the API image never needs streamlit/plotly/reportlab.
pandas==3.0.2
fastapi
pydantic
uvicorn
python-multipart
mysql-connector-python
# --- authentication (auth.py) ---
# bcrypt: password hashing. Session tokens are plain random strings
# (secrets.token_urlsafe), so no JWT library is needed.
bcrypt
